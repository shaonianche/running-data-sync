[project]
name = "running-data-sync"
version = "1.0"
description = ""
authors = [
{name = "duanfei", email = "duanfei.job@gmail.com"},
]
dependencies = ["httpx==0.24.1", "gpxpy==1.4.2", "stravalib", "appdirs>=1.4.0", "svgwrite>=1.1.9", "colour", "s2sphere", "arrow", "geopy", "polyline", "sqlalchemy", "timezonefinder", "pyyaml", "aiofiles", "cloudscraper==1.2.58"]
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pdm]
[[tool.pdm.source]]
name = "pypi"
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
verify_ssl = true

[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"
